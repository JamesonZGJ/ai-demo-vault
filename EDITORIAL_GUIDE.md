# 案例编辑与发布规则

## 1. 内容对象

每个案例必须同时包含四类信息：

1. 官方事实：产品能力、技术、许可、价格或公开状态。
2. 编辑拆解：痛点、用户、商业逻辑和实现路径的分析。
3. 产品假设：尚未验证的用户、盈利和二次创新方向。
4. 素材权利：截图、封面、视频、GIF、Logo 和代码的使用边界。

三类文字在页面上必须明确区分；编辑判断和产品假设不能伪装成官方事实。

## 2. 必填字段

| 组 | 必填内容 |
|---|---|
| 身份 | slug、名称、短句、摘要、分类、案例形态、技术披露状态、源码状态；已核验工具可为零个或多个 |
| 发布 | 状态、发布方地区、来源平台、实现成熟度、收录日期、核验日期 |
| 展示 | 原创或已授权封面、至少一份获准使用的真实产品截图/GIF/视频、alt、在线体验/仓库/文档链接 |
| 拆解 | 痛点、解决方案、目标用户、核心功能、AI 实现、技术实现、盈利方式 |
| 改造 | 改变谁、改变什么、改变场景、新机会假设四个独立字段及理由 |
| 证据 | 每个事实主张、来源链接、核验日期 |
| 判断 | 三档实现难度、商业潜力 1–5、规则版本与理由 |
| 权利 | 版权方、许可、来源、授权状态和限制 |

## 3. 实现成熟度

| 值 | 判断标准 |
|---|---|
| `concept` | 只有概念、静态稿或不可操作展示 |
| `interactive_prototype` | 可以操作，但核心后端、AI 或真实数据仍为模拟 |
| `working_demo` | 核心 AI 输入—处理—输出流程真实可运行 |
| `production_product` | 官方提供稳定可用的正式产品或服务 |

开源、公开采用、公开价格和商业服务是独立证据标签，不属于成熟度。

案例形态另行区分：`product` 是独立产品，`open_source_project` 是开源项目，`model_demo` 是模型与体验入口，`official_template` 是官方模板，`platform_workflow` 是平台内工作流，`embedded_feature` 是产品内嵌能力。案例形态不等于成熟度，也不能把仓库、模型或模板写成正式 SaaS。

## 4. 实现难度

| 等级 | 判断标准 |
|---|---|
| `beginner` | 单一输入输出；可直接使用成熟 API；无队列、复杂权限或媒体管线 |
| `intermediate` | 需要账号、存储、多步骤任务、至少一个外部模型或业务集成 |
| `advanced` | 需要 GPU/自托管模型、长任务队列、视频管线、多租户、复杂平台 API 或明显合规投入 |

难度必须按“复刻核心闭环”的最小真实实现判断，不能按原产品全部规模判断。

## 5. 商业潜力编辑判断

商业潜力只是站内编辑判断，不等于收入预测。

| 分数 | 判断标准 |
|---:|---|
| 1 | 付费者、频率和价值都不清楚，主要是展示型体验 |
| 2 | 痛点存在，但付费场景弱或获客路径不清楚 |
| 3 | 目标用户、痛点和一种合理付费方式清楚 |
| 4 | 高频或高价值场景明确，并有公开定价、商业服务或可核验替代成本 |
| 5 | 同时有清晰付费者、重复价值、公开商业信号和可执行细分切入点 |

每个公开案例的分数必须写 1–3 句理由并附证据；缺分数、理由、规则版本、日期或依据时不能发布。MVP 不计算五维综合创业分。

## 6. 来源与主张

| 类型 | 规则 |
|---|---|
| `fact` | 必须关联官方产品页、官方文档或官方仓库 |
| `editorial_inference` | 必须显示“编辑推断”，并说明推断依据 |
| `hypothesis` | 必须显示“产品假设”，不得使用“已验证” |

CSDN、掘金、Product Hunt 讨论等可以用于发现线索；关键事实应回到一手来源。平台点赞、GitHub stars、榜单和本站收藏不能互相替代。

每个公开详情字段必须有且只有一条主要主张，并把标签放在字段正文旁边。首版覆盖产品概述、成立原因、痛点、解决方案、目标用户、核心功能、AI 实现、技术实现、盈利方式、实现边界和创业改造共 11 个区块。`fact` 必须同时显示一手来源和核验日期；`editorial_inference` 与 `hypothesis` 不得借用官方链接制造“官方已确认”的错觉。主张类型按正文实际含义判断，不能因为字段名称固定就统一写成事实；例如“复刻需要哪些架构”通常是编辑推断。

源码状态独立于案例形态、成熟度和素材授权：

| 状态 | 公开规则 |
|---|---|
| `open_source` | 必须有 canonical 仓库链接、开源证据和可核验许可证；页面写“源码已公开”，但不暗示截图、商标、模型或第三方依赖可自由商用 |
| `closed_source` | 已确认源码未开放；不得显示仓库入口或开源证据，也不得暗示本站可以出售源码 |
| `not_disclosed` | 尚未确认，保持草稿；不能用“闭源”替代未知状态 |

ColorSnap 当前是站长自有的非 AI 交互原型：必须明确写出没有 AI 模型调用，并把 CIEDE2000 标为确定性颜色计算，而不是模型推理；已实现、模拟和未实现能力分别记录。它在取得完整公开证据前保持草稿，源码状态为 `closed_source`。

## 7. 素材发布

素材状态只允许：`pending`、`approved`、`rejected`。

- `approved`：原创素材，或许可/授权明确允许本站使用。
- `pending`：尚未核对，不进入公开页面。
- `rejected`：不允许使用，保留原因但不进入构建产物。

卡片封面可以使用本站原创抽象视觉，但详情页仍必须有至少一份真实产品展示媒体。该媒体只能由本站在许可允许范围内实际运行后采集，或取得权利人明确授权；公开可访问不等于允许商用转载。无法取得合格展示媒体的案例保持草稿。ColorSnap 的 `.chrome-profile` 永不进入项目；现有 Unsplash 图片在逐图补齐作者与原始链接前保持 `pending`。

- 图片和 GIF 必须有有效 alt；GIF 同时准备静态封面，减少动态效果时只显示静态版本。
- 视频默认无声、可暂停；有口述信息时必须有字幕或等价文字摘要。
- 没有已批准素材时不留空白媒体槽；加载失败只显示真实错误，不换用未授权图片。

## 8. 发布门槛

只有同时满足以下条件才能从草稿变为公开：

- 官方主来源、HTTPS URL 和核验日期存在。
- 至少一张 `approved` 封面和有效 alt，并有至少一份 `approved` 的真实产品截图/GIF/视频；纯抽象封面不能代替产品展示。
- 所有 `fact` 主张均有来源。
- 11 个详情区块各有一条主要主张，类型与正文一致；事实附一手来源和核验日期，推断与假设明确标注。
- 源码状态已确认；`open_source` 与仓库、开源证据一致，`closed_source` 不挂仓库或开源证据，`not_disclosed` 不发布。
- 成熟度、难度、分类和发布方地区已确认；`mixed` 主体逐项记录，`unknown` 不发布。
- 商业潜力分数、理由、依据、规则版本和核验日期全部存在。
- 没有伪造收藏、收入、用户量或商业成功。
- 在线体验、仓库和文档链接已人工打开核验。
- 版权、商标或隐私阻断项为零。

公开后每 90 天复核链接和核心事实；失效且无法确认的案例转为归档，不继续显示为可在线体验。

## 9. 候选采编状态

来源平台只能生成候选，不能直接生成公开案例。状态按证据推进，不允许跳级：

| 状态 | 进入条件 | 可以做什么 |
|---|---|---|
| `discovered` | 只有发现 URL 和发现时间 | 排队核验，不展示、不打分 |
| `source_verified` | 发布方、发布方地区、官方主来源和来源平台已确认 | 摘录官方事实，仍不公开 |
| `facts_ready` | 事实逐条有来源，成熟度和许可边界已记录 | 写编辑拆解与创业改造 |
| `editorial_ready` | 难度、商业潜力、四项创业改造和内容标签完整 | 制作原创或获准素材 |
| `publish_ready` | 发布门槛全部通过 | 才允许数据库事务切换为 `published` |
| `rejected` | 主体不可确认、事实不符、重复、许可或隐私风险无法接受 | 保存原因和日期，不进入公开数据 |

`pending` 不能被解释为“默认通过”。任何链接、许可或主体核验失败都必须显式停在对应状态。

## 10. 去重规则

- 同一官方产品、仓库或服务只建一个案例；地区站点、语言页和多个发现渠道挂到同一案例链接下。
- 同一公司不同 AI 功能只有在用户任务、核心闭环和官方产品入口都独立时才拆成不同案例。
- fork、换壳、镜像和社区转载不作为新案例；必须回到上游官方仓库。
- 重命名产品保留稳定内部 ID、旧名称和核验记录，不重复累计收藏。
- 无法确定是否同一产品时保持候选并人工判断，不用标题相似度自动合并。
