# 骨架屏加载接入指南

1. 用真实业务状态控制 `loading`，不要在组件内部猜测请求是否完成。
2. 骨架结构应接近最终内容的头像、标题、正文和操作区。
3. 给容器提供稳定最小高度，避免内容出现时产生布局跳动。
4. 加载期间使用 `aria-busy="true"`，完成后切回 `false`。
5. 流光只作为辅助反馈；减少动画偏好开启时必须关闭。
6. 失败和空状态需要独立页面，不要长期停留在骨架屏。

验收：加载和内容尺寸接近、减少动画有效、360px 无横向溢出。
