# Claude 提示词：骨架屏加载

为 React + TypeScript 产品实现 `SkeletonLoader`，用于项目列表、AI 历史记录和数据卡片。

- 使用受控 `loading` 状态。
- 骨架外形要对应最终内容结构。
- 宽度序列必须确定，不使用随机数。
- 通过 `aria-busy` 表达更新中的区域。
- 流光需要支持减少动画偏好。
- 加载、空状态和错误状态保持独立。
- 兼容 360px 移动端。

交付组件源码、样式、参数说明、接入示例和测试。
