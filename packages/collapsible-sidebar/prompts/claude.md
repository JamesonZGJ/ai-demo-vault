# Claude 提示词：可折叠侧边栏

为 AI 工作台实现可复用侧边导航：

- 展开时展示品牌、标题、徽标和账户
- 收起时保留图标、当前项和辅助名称
- 移动端使用遮罩抽屉
- 支持 Escape 关闭
- 使用 `nav`、`aria-current`、`aria-expanded` 和 `aria-controls`
- 业务层控制当前项与开关状态
- 不使用随机标识，不伪造路由
- 360px 无横向溢出

交付源码、样式、参数说明、接入指南和测试。
