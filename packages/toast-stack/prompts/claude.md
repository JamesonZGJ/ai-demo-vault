# Claude 提示词：堆叠通知

为 React + TypeScript 产品实现 `ToastStack`，用于保存成功、生成完成、上传失败和后台任务提醒。

- 使用受控通知数组和稳定唯一 `id`。
- 折叠状态表达层级，展开状态保证内容可读。
- 新通知进入、旧通知后退、关闭后补位。
- 最多显示 1–6 条，其余通知对辅助技术隐藏。
- 支持状态文字、进度、操作与关闭。
- 使用 `aria-live`，兼容键盘和减少动画偏好。
- 兼容 360px 移动端。

交付组件源码、样式、参数说明、接入示例和测试。
