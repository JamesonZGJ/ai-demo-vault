# 堆叠通知接入指南

1. 在业务层维护唯一通知 `id`，新通知插入数组开头。
2. 用 `onDismiss` 明确删除指定通知，不做不可追踪的盲切换。
3. 自动关闭计时由业务层管理；Hover、聚焦或页面隐藏时应暂停计时。
4. 同一时间最多展示 3～4 条，更多通知进入通知中心或历史记录。
5. 成功、提醒和失败只表达真实业务结果，不用颜色代替文字。
6. 操作按钮必须能用键盘触发，关闭按钮需要可读名称。
7. 减少动画偏好开启时关闭位移和缩放过渡。

验收：新增、展开、关闭和补位连续；键盘可用；360px 无横向溢出。
