# 商业化基础契约

本文件约束后续商品商业化 change。DemoPreview 是长期公开的发现入口，CapabilityPackage 是主售商品，Bundle 是模块组合商品，App Blueprint 是高级商品；Entitlement 是订单或有效会员产生的访问与下载权。当前代码已落地 ColorSnap Blueprint #001 的本地预览商品、访问授权和样品下载闭环，但没有生产商品订单、支付、会员或真实付费下载；模块价格 ¥1.9–9.9 与其他价格均为待验证假设，不能直接成为生产结账价格。

## 首阶段销售边界

首阶段只支持本站作为唯一销售方的精选 Demo Marketplace，不开放第三方卖家入驻。若后续允许创作者销售，必须另建卖家主体、合同、KYC、商品审核、分账、提现、税务、侵权下架、争议和平台佣金，不能复用“本站自营”字段假装支持多卖家。

公共商城只有在至少一个自营 Blueprint 于生产环境完成真实购买、服务端支付确认、权益、私有下载、退款/撤权和逐笔对账后才能声明可成交。页面存在、沙箱支付或点击购买都不能替代该证据。

## 权益边界

| 层级 | 永久不变的边界 | 待确认内容 |
|---|---|---|
| 免费层 | Demo 介绍、代表媒体、基础拆解和证据页面保持公开，用于 SEO；不能上线后突然整体锁进会员 | 免费与深度商业分析的准确字段分界 |
| Capability Package 单品 | 只授予一个明确模块版本及其客户许可；源码必须是本站自研重实现，不授予第三方闭源产品源码 | 每个商品的 ¥1.9–9.9 精确价格、更新期限、席位和退款规则 |
| Bundle | 只授予清单内每个 Capability Package 的明确版本及统一客户许可 | Apple UI、Game UI、AI Agent 等组合价格、更新期限和退款规则 |
| App Blueprint | 只授予一个明确完整产品版本及其客户许可，不授予第三方闭源产品源码 | ColorSnap 等商品的精确价格、更新期限、席位和退款规则 |
| AI Builder Pass | “全部 Demo 库”解释为免费目录之外的深度商业分析、可售 Blueprint 与 Prompt 更新；免费目录本身不是付费权益 | USD 9.9/月是否成立、每周新增承诺、取消后历史版本访问和宽限期 |
| Pro 创业版 | “全部源码”只指本站拥有或明确获准再分发的源码；一次咨询和定制方向必须形成可审计交付 | USD 199 是一次性还是周期、咨询时长、预约期限、改期和退款 |

## Capability Package 可售门槛

每个可售 Capability Package 必须脱离 ColorSnap 单独成立：

- 有本站自研重实现的源码，不能直接转售 CodePen、Aceternity UI、Magic UI、ReactBits 或 Motion Primitives 的第三方源码。
- 有 Cursor Prompt、Claude Prompt、README、参数说明和 Integration Guide；Codex/Trae Prompt 作为后续可选资产。
- 每个资产记录版本、格式、来源、许可证、归属、独立重实现说明和可再分发范围。
- DemoPreview 可以公开但不代表 Package 已交付；缺任何必需资产或权利记录时，只能显示 Preview，不能显示购买按钮。
- Package 必须能说明输入、输出、接入点、依赖和已知限制，确保用户买的是可复用能力而不是源码压缩包。

## App Blueprint 可售门槛

八类交付物都是原始承诺中的“包含”，因此默认全部必需：

```text
prompt
prd
ui_design
figma_file
html_demo
react_source
data_schema
monetization_design
```

每个 `sellable` 版本还必须包含：

- 根目录 README、版本号、更新说明、逐文件 manifest、每个文件的字节数与 SHA-256，以及最终压缩包 SHA-256。
- 本站客户许可、第三方声明、依赖及素材许可清单。
- 启动、构建、导入数据结构和已知限制说明。
- 密钥与隐私扫描结果；不得包含浏览器 Profile、账号、Token 或未授权媒体。
- Figma 必须交付实际文件或明确允许离线交付的等价资产，不能只售外部链接。
- 冻结版本时必须从实际压缩包重新列出全部文件，并与数据库 manifest 逐路径、大小和哈希完全一致；多文件、少文件或任何字节变化都失败。
- 文件权利、密钥扫描、构建、导入和可读性验证保存为不可变批次与逐项结果，不能只在版本行写一个 `verified=true`。

## 最小数据对象

| 对象 | 必需字段 |
|---|---|
| `blueprints` | 稳定 ID、名称、slug、作者/销售方、内容审核状态、公开可见性、目标用户、结果承诺、公开时间 |
| `blueprint_demo_links` | Blueprint、Demo、`own_case/original_rebuild/licensed_derivative` 关系、原创/许可声明、审核日期 |
| `offers` | 稳定商品身份、类型、名称、销售方和生命周期状态；不原地保存可变政策 |
| `offer_versions` | Offer、版本、资源规则、席位、访问期限、更新、支持、迟到付款处理、内容哈希、生效时间和状态 |
| `offer_resources` | Offer Version、明确 Blueprint 版本/集合、更新范围、许可范围和访问期限 |
| `policy_versions` | 客户许可/服务条款/退款/隐私等类型、语言、版本、完整内容哈希、生效/失效时间和批准状态 |
| `offer_policy_versions` | Offer Version、政策类型、Policy Version、是否必须在结账接受；同一 Offer Version 与类型唯一 |
| `prices` | Offer Version、最小货币单位金额、币种、一次性/周期、周期单位、含税方式、生效/失效时间、价格版本 |
| `provider_price_refs` | 内部价格、支付商、商户账号、外部产品/价格 ID、环境和状态 |
| `blueprint_versions` | Blueprint、版本、状态、发布日期、更新说明、私有包键、包大小、包 SHA-256 和 manifest 哈希 |
| `blueprint_artifacts` | 版本、八类枚举之一、逻辑名称、格式、必需性和展示顺序 |
| `blueprint_files` | 版本、Artifact、包内规范路径、字节数、SHA-256、MIME/格式和文件角色；版本内路径唯一 |
| `artifact_rights` | Blueprint File、权利人、来源、许可版本、证据、商用/修改/再分发/署名规则、审核人和日期 |
| `validation_runs` | Blueprint 版本、验证器版本、环境、开始/完成时间、manifest 哈希、结果和不可变日志摘要 |
| `validation_results` | 验证批次、Blueprint File/包、检查类型、预期、实际、结果和证据哈希 |
| `checkout_sessions` | 用户、Offer、Price、内部订单、支付商会话、状态、失效时间和幂等键 |
| `provider_customers` | 用户、支付商、商户账号、外部客户 ID、环境和去标识化状态 |
| `merchant_accounts` | 私有 schema 中的支付商、销售主体、环境、币种能力、状态和密钥引用；不保存明文密钥 |
| `orders` | 用户、状态、币种、小计、税费、总额、成交时间和订单级不可变快照哈希 |
| `order_items` | Offer Version、Price、Blueprint 版本、金额、权益和整组适用政策的不可变快照哈希 |
| `order_item_policy_versions` | Order Item、政策类型、Offer Policy Version、Policy Version、内容哈希和是否已要求接受；订单确认后不可修改 |
| `payment_attempts` | 支付商、内部请求号、外部交易号、金额、币种、状态、失败原因和时间 |
| `payment_transactions` | 不可变 capture、refund、chargeback、fee、payout 等交易类型、金额、币种、外部对象和发生时间 |
| `payment_exceptions` | 支付尝试/交易、`late_capture/state_conflict/provider_unavailable` 类型、绑定的固定处理规则、状态和解决结果 |
| `provider_events` | 支付商事件 ID、验签结果、载荷哈希、接收时间和处理结果；支付商与事件 ID 唯一 |
| `refunds` | 订单、金额、原因、状态、外部退款号、发起和完成时间 |
| `refund_allocations` | Refund、订单项、金额、税额和受影响 Entitlement Resource；分配总额必须等于退款额 |
| `disputes` | 支付交易、外部争议号、金额、原因、状态、证据期限和结果 |
| `membership_plans` | 稳定方案身份、名称和生命周期状态 |
| `membership_plan_versions` | 方案、版本、领取额度、重置周期、可领取集合、更新范围、到期后重下规则、内容哈希和生效时间 |
| `membership_claims` | 用户、订阅、方案版本、领取周期、Blueprint 版本、消耗额度、生成权益和时间；同一幂等键唯一 |
| `subscriptions` | Membership Plan Version、支付商订阅号、状态、当前周期、期末取消、扣款失败与宽限期 |
| `subscription_cycles` | 订阅、账期、应付/实付金额、币种、支付交易、状态和权益有效期 |
| `entitlements` | 用户、资源范围、来源订单/订阅、开始/结束时间、状态和撤销原因 |
| `entitlement_resources` | 权益、明确 Blueprint 版本/集合、更新范围、许可版本和访问期限 |
| `download_assets` | Blueprint 版本、私有路径、大小、SHA-256 和可用状态 |
| `download_grants` | 用户、权益、资产、授权范围、签名地址失效时间、状态和幂等键 |
| `download_events` | 授权、可观察动作、结果、IP 风险摘要和时间；签发地址不得记为文件已完整下载 |
| `consultations` | Pro 权益、状态、时长、时区、预约期限、改期次数、范围、预约时间、交付物和完成时间 |
| `terms_acceptances` | 用户、订单、Offer Policy Version、Policy Version ID、内容哈希、语言、同意时间和来源；同一订单与政策类型唯一 |
| `pricing_decisions` | 实验口径、预设阈值、真实结果、最终精确价格、批准人和日期 |
| `reconciliation_runs` | 支付商、商户账号、币种、结算区间、状态、差异数量和完成时间 |
| `reconciliation_entries` | 对账批次、内部交易、外部交易、期望/实际金额、差异类型和处理结果 |

## 状态与不变量

| 范围 | 必须成立的规则 |
|---|---|
| 商品 | 只有 `sellable` 且权利检查通过的固定版本可以加入订单 |
| 内容与销售状态 | Blueprint 内容审核、公开可见性、版本可售性、Offer 状态和 Price 状态分别保存，不能共用一个 `status` |
| Blueprint 版本 | 进入 `sellable` 后版本号、包清单、文件和哈希不可原地修改；更新必须创建新版本 |
| Offer/政策版本 | 激活后资源、许可、退款、更新、支持和迟到付款规则不可修改；变更创建新版本，订单引用固定 ID 与内容哈希 |
| 价格版本 | 激活后金额、币种、周期和含税口径不可修改；调价创建新价格，旧订单保持原快照 |
| 订单 | 订单只表达创建、待支付、确认、取消和过期；退款与争议用独立对象表达，客户端成功页不能改为已支付 |
| 支付 | 只有验签通过且金额、币种、商户和订单一致的服务端事件可以授予权益 |
| 回调 | 重复、延迟、乱序和重放事件不得重复授权、退款或记账 |
| 事务 | 支付确认、订单确认、权益创建和事件完成必须在同一数据库事务中完成，失败整体回滚 |
| 权益 | 明确绑定版本或版本集合；页面、接口和私有存储统一执行服务端检查，短期签名地址不能替代权益判断 |
| 订阅 | 生效、续费、期末取消、扣款失败、宽限、到期、恢复和退款撤权均有测试 |
| 下载 | 未授权用户无法猜测对象地址；每次签名地址短期有效并写审计事件 |
| 咨询 | 一次权益最多完成一次；预约、改期、完成、过期和交付均有记录 |
| 退款 | 退款或许可撤销只能阻止未来下载并更新许可状态；系统不能声称删除用户设备里已经下载的文件 |
| 金额一致 | 前台显示、支付请求、支付回调和订单快照金额必须完全一致，不能用 USD 19–39 区间结账 |
| 金额 | 所有金额使用整数最小货币单位，币种使用三位大写代码，`subtotal + tax = total` |
| 用户删除 | Auth 用户删除不得级联删除财务和条款记录；按法律与政策保留并去标识化 |
| 对账 | 订单、capture、refund、chargeback、fee 与支付商结算按商户和币种逐笔一致 |

## 权限与服务端边界

| 区域 | 规则 |
|---|---|
| 公开商品 | 游客只读已批准、公开且版本可售的摘要；Offer 暂停时只显示暂停销售，只有激活 Offer/Price 可购买；不返回私有存储键或原始权利证据 |
| 用户数据 | 登录用户只能读取自己的订单、权益、订阅、条款接受和下载授权 |
| 客户端写入 | 客户端不能直接插入或更新订单、支付、退款、订阅、权益、支付事件或对账记录 |
| 支付私有数据 | 商户账号、外部交易、回调载荷和对账放入未公开私有 schema |
| 私有文件 | Bucket 永不公开，匿名和登录用户都没有直接 `storage.objects` 读取策略 |
| 服务端特权 | 普通页面继续不持有 Service Role；只有隔离的 `server-only` 支付、回调和下载模块可调用窄范围特权入口 |
| 数据库特权 | 特权 RPC 固定安全 `search_path`，撤销 anon/authenticated 执行权，并在事务内锁定订单/订阅行 |

## 支付事件处理

1. 使用原始请求体、正确商户密钥和时间窗口验签；失败事件不进入业务处理器。
2. `(provider, merchant_account, event_id)` 唯一；重放返回既有结果。
3. 通过外部对象找到内部支付尝试并锁定订单或订阅。
4. 同时校验商户、金额、币种、订单快照和支付对象；任何不一致失败即停，不授予权益。
5. 延迟或乱序事件不能让状态倒退；必要时向支付商查询权威状态并记录来源。
6. 浏览器返回页只读取服务端状态，不能确认付款或创建权益。
7. 退款成功事件才撤销未来访问；争议、退款和订单状态分别保存。
8. 定期拉取交易与结算数据，按商户和币种逐笔对账。

### 冲突与迟到事件规则

| 场景 | 确定处理 |
|---|---|
| 过期订单收到有效 capture | 记录不可变交易并进入 `late_capture` 异常；按订单绑定的 Offer Version 固定规则选择履约或全额退款，不读取当前页面政策 |
| 同一支付事件重放 | 返回原处理结果，不重复改变订单、权益、退款或账本 |
| 已确认订单收到失败/取消事件 | 不让订单倒退；保存事件并在支付商状态冲突时查询权威对象，冲突进入对账差异 |
| 部分退款 | 必须有 `refund_allocations` 逐订单项与权益资源分配；首笔单品不支持无法映射的部分退款 |
| 订单项全额退款 | 撤销该订单项产生的未来下载与更新权益，不影响同单其他未退款资源 |
| 争议开启 | 对对应权益资源进入 `suspended`，阻止新下载并保留审计；不声称删除已下载文件 |
| 争议胜诉 | 创建恢复记录并重新激活仍在期限内的资源；不改写旧暂停记录 |
| 争议败诉 | 撤销对应资源并保存 chargeback、fee 和对账结果 |
| 订阅补扣成功 | 只按支付商确认的账期创建或恢复该周期权益；不能把过期周期无限补成当前访问 |
| 无事件顺序或版本号 | 在应用冲突状态前向支付商查询权威对象；查询失败保持 `processing_exception`，不猜测最终状态 |

## 支付覆盖

| 渠道 | 计划币种 | 生产完成证据 |
|---|---|---|
| Stripe | USD | 生产商户启用；最小金额成功、取消、失败、退款、回调重放和逐笔对账 |
| 支付宝 | CNY | 明确网页支付产品与大陆商户能力；生产交易、退款、验签、幂等和对账 |
| 微信支付 | CNY | 明确 Native/H5/JSAPI 等实际产品与商户能力；生产交易、退款、验签、幂等和对账 |

沙箱或审核环境是开发证据，不是终局完成证据。生产小额交易、退款或商户开通都属于外部状态变更，执行前必须取得用户明确授权。

## 首笔交易必须通过的验收

| 范围 | 验收证据 |
|---|---|
| 1 个包 | Blueprint #001 八类 Artifact 和全部 Blueprint File 齐全；实际压缩包与 manifest 完全一致 |
| 可复刻 | 从实际下载包全新解压；HTML 可打开、React 可构建、数据结构可导入，文档和 Figma 可读取 |
| 权利与安全 | 每个包内文件均 `approved_for_sale`；权利、密钥和隐私验证批次通过 |
| 真实交易 | 一个精确价格、一个币种和一个生产支付商完成成功、失败、取消、重放、迟到、退款和逐笔对账 |
| 权益与越权 | 支付确认后生成正确版本权益；未购买者无法从页面、接口或存储绕过；退款只撤销对应资源 |
| 政策证据 | 订单与接受记录引用固定 Offer Version、Price 和 Policy Version，内容哈希可复核 |

## 完整商业目标必须通过的验收

| 范围 | 验收证据 |
|---|---|
| 20 个包 | 数据库恰有 20 个 `sellable` Blueprint；八类 Artifact 和逐文件 manifest 逐包齐全 |
| 可复刻 | 从实际下载包全新解压；HTML 可打开、React 可构建、数据结构可导入空库，其他文档和 Figma 可读取 |
| 权利 | 每个包内文件均为 `approved_for_sale`，没有第三方闭源源码、未授权素材或密钥 |
| 越权 | 未购买者通过页面、接口和存储地址均不能下载；购买者只能访问被授予的版本 |
| 会员 | 生效、续费、取消、失败、宽限、到期和恢复端到端通过 |
| Pro | 一次咨询不能重复消费；预约与最终建议交付均可追溯 |
| 支付安全 | 跳转成功页不授权；回调验签、金额币种校验、幂等、乱序和退款测试通过 |
| 对账 | 内部订单、支付商交易、退款和最终到账按币种逐笔一致 |
| 每周更新 | 如果保留为付费承诺，先定义香港时区、最低数量和断更处理，再连续四周证明履约 |
| 条款 | 结账保存客户许可、退款政策和服务条款的实际接受版本 |

## 开发前必须由用户决定

1. 首阶段是否继续限定本站自营精选商店；销售与收款主体、Stripe/支付宝/微信商户账号是否具备生产资格。
2. 每个单品的精确价格；Builder Pass 的权益和取消后访问；Pro 是一次性还是周期产品。
3. 数字文件已下载、会员续费、咨询已预约/已完成时的退款规则。
4. 税费是否含价、收据或发票流程，以及服务国家/地区限制。
5. 客户许可允许几席、能否商用修改、能否为客户交付，以及禁止再分发的边界。
